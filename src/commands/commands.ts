import ping from './ping';
import poll from './poll';
import mute from './mute';
import course from './course';
import pfp from './pfp';
import bot from './bot';

const commandsList = [ping, mute, poll, course, pfp, bot];

export default commandsList;
