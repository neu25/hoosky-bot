import enable from './enable';
import disable from './disable';
import add from './add';
import remove from './remove';

const subcommands = [enable, disable, add, remove];

export default subcommands;
