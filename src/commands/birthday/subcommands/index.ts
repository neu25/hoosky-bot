import { list } from './_list';
import { set } from './_set';
import { show } from './_show';
import { unset } from './_unset';

export const subcommands = [list, set, show, unset];

export default subcommands;
