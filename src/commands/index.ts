import ping from './ping';
import poll from './poll';
import { mute, unmute } from './mute';
import { newclass } from './classes';

const commands = {
  ping,
  mute,
  unmute,
  poll,
  newclass
};

export default commands;
