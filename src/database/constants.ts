/**
 * Collection names
 */
export enum Collection {
  CONFIG = 'config',
  POLL = 'poll',
  COURSES = 'courses',
  MAIL = 'mail',
  BIRTHDAYS = 'birthdays',
}

/**
 * Document IDs within the `config` collection
 */
export enum Config {
  GUILD = 'guild',
  ROLES = 'roles',
  MAIL = 'mail',
  BIRTHDAYS = 'birthdays',
}
