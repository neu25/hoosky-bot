/**
 * Collection names
 */
export enum Collection {
  CONFIG = 'config',
  COURSES = 'courses',
  MAIL = 'mail',
}

/**
 * Document IDs within the `config` collection
 */
export enum Config {
  GUILD = 'guild',
  ROLES = 'roles',
  MAIL = 'mail',
}
