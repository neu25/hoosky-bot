/**
 * Collection names
 */
export enum Collection {
  CONFIG = 'config',
  COURSES = 'courses',
  BIRTHDAYS = 'birthdays',
}

/**
 * Document IDs within the `config` collection
 */
export enum Config {
  GUILD = 'guild',
  ROLES = 'roles',
  BIRTHDAYS = 'birthdays',
}
